apiVersion: v1
kind: Service
metadata:
  name: amori-backend-service # <--- 'hello-world-service'에서 수정
spec:
  type: NodePort
  selector:
    app: amori-backend # <--- 'deployment.yaml'의 라벨과 일치
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080